# Live music agent config
# API keys: set in .env (not committed):
#   OPENAI_API_KEY, SERPAPI_API_KEY, SPOTIFY_CLIENT_ID, SPOTIFY_CLIENT_SECRET

agent:
  # model is auto-selected based on provider (set LLM_MODEL in .env to override)
  temperature: 0.5

# Optional: add more sections later
